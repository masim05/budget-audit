# Report Examples

Generated reports can be written to local files with:

```bash
budget-audit audit --format json --output examples/reports/sample-report.json
```

Reports are service files only. They are not backed by a database and do not require any network or MCP service.
